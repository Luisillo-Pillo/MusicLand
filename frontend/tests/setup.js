// Se carga una vez antes de cada archivo de pruebas (ver vite.config.js →
// test.setupFiles). Extiende expect() con los matchers de jest-dom
// (toBeInTheDocument, toHaveTextContent, etc.) que usan las pruebas de
// componentes.
import '@testing-library/jest-dom/vitest';
